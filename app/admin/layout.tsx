export const metadata = {
  title: "LotChat Admin Panel",
  description: "Admin control panel for LotChat",
}

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
